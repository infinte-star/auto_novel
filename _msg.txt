D4: 文档改写成描述现役 v2 引擎，并把删除留下的坑量化记下来

`95361b9` 删掉 v1 之后，四份活文档还在描述一台不存在的引擎。这次改写
`CLAUDE.md` / `README.md` / `docs/INTERNALS.md`，并给两份历史文档挂上历史声明。

**改写而不是删段**：每个被删机制的位置上写的是「今天由谁承担」，
因为 v1 的规则大半是测出来的，删掉描述会让下一个人重新踩一遍。
例：`_EXECUTABILITY_DOCTRINE` 随 planning.py 消失，但它用散文强制的「具体性」
现在是 `arc.ARC_SYSTEM` 规则 2——正是那条让卡片字段可 grep、从而让 CCC
零 LLM 可判定的规则。

**新增 CLAUDE.md「Known gaps left by the v1 deletion」一节。** 删除的账不能
只记贷方：
- **12 个已注册的门没有调用点了**。`GateRegistry` 从不派发（`REGISTRY.get()`
  只有测试在调），v1 的 review.py 是按名字逐个调的。8 个 advisory + 4 个有修复层
  的门因此静默。按 LESSONS §4「静默的门是缺陷报告，不是删除候选」，
  这一节点名列出并要求逐个 `gate_census` 测过再定接或删——**没有顺手删掉**。
  只有 `beat_coverage` 已结案：被 `accept.contract_fulfilment` 从 beats 扩到整卡后取代。
- 约 2.2k 行其它孤儿定义 + 约 14 个无读者配置键。
- **两个能力缺口**（审计时才发现，不是死码）：`memory/opening_route.md` v2 完全
  没读，`adopt-trial` 对写作侧已失效；`voice_baseline.md` 不再有生产者，
  v2 因为没有 voice 刷新路径而天然冻结——结论对，文件消失了。
  两条都在 README 用户侧同步标注（别让用户以为 adopt-trial 还在生效）。

**订正一个我自己差点写进文档的数字。** A/B 那一栏原本按 `fpy_prime` 的原始读数
写成 73% → 97%。那 13pt 里 6 次失分全是 `book_wide_fossils` 硬拒，
而这个门今天在首稿上根本不可能触发（§9.7 已根因）——算成 v2 的胜利就是拿一个
已修 bug 冒功。文档里改成 replay_gates 的归一化读数 **86.7% → 96.7%**，
并注明剩下那 10pt 全部来自写前的规划/卡片重试次数（3:1），
不是「v2 写的稿更容易过门」。

**docs/INTERNALS.md 的 checkpoint 布局按真实产物核对过**，不是照 v1 那张表改名：
实跑目录里没有 `chapter_saved.json` / `chapter_current_v2.md`（草稿在
`chapter_draft.json` 里），`arc_generated.json` 只在弧首章，
`card_patch.json` 在干净的 30 章里是 0 个文件（只有 canon 出 `target: next_card`
结论才写）。

历史文档不改写内容，只挂声明：`REDESIGN.md` 和 `docs/LESSONS.md` 是当时那台
引擎上的测量记录，改写成符合今天的代码等于销毁证据。
`docs/REDESIGN_V2.md` 追加 §9.8 记录处置执行（含 §9.7 结尾那句「默认仍是 v1」已作废），
以及方法论收获：大删除之后找孤儿必须按 AST 收集引用——v2 的 docstring 大量提到
v1 的函数名，token grep 会把散文计成引用，恰好藏起被删除孤立的那些函数
（24 个/~860 行 → 47 个/2,250 行）。

805 个测试全绿（纯文档改动，无代码变更）。
