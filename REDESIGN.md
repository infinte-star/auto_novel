# Pipeline 重新设计 v1 —— 质量优先 / 首过率导向

> **后续版本：`docs/REDESIGN_V2.md`。** v2 保留本文件的诊断与 L2–L6 杠杆，
> 但先于它们修一件本文件假定成立的事：FPY 定义在 `quality_threshold: 8.0` 上，
> 而 8.0 恰是全库自评的中位数，这条自评又被实测证明无判别力——
> 所以本文件的 P2/P4 判据在原理上无法结算（见 v2 §0）。
> 本文件继续作为 P1–P4 的执行记录保留。

> 目标函数：**最大化「首过率」（First-Pass Yield, FPY）= 一次规划、一次写作、零重来即达标的章节占比。**
> 成本不是目标，但省下的算力必须**原地再投资**到写作那一次调用上。
> 所有数字来自 `novels/*/logs/llm_calls.jsonl`（2879 次真实调用）+ `telemetry/global.db`（1021 章），可复现。

---

## 0. 实测基线：首过率 12%–63%，中位 38%

> **2026-07-27 更新**：首过率已从「按 `llm_calls.jsonl` 里 `write==1` 估算」
> 改为从 `logs/checkpoints/chNNNN/` 的产物精确统计（`novel.py stats` 现直接打印）。
> 旧口径把结构性多稿（`candidate_chapters: 2`）误判成返工，低报严重——
> huangliang 旧算法 0%，实测 58%。下表是实测值。
>
> 「首过」定义：单次规划尝试、无 replan、单轮 review、无重写/补丁/钩子重做、无 quality-debt。

```
tunshi_xitong          33/52  = 63%
huangliang             58/100 = 58%
tangshuting_v1_backup 111/200 = 56%
tangshuting            76/200 = 38%
guize_guaitan           4/12  = 33%
tangshuting_e2e         7/45  = 16%
yeban_guize             3/25  = 12%   ← post-MVP，配置已是全 1
```

**返工原因在每一本里都是同一个：`replan` 排第一**（占章数 33%–68%），
其次是 `revise` / `quality_debt`。也就是说返工的主战场在**规划**，不在写作——
这与 §1 根因 B/C 指向的方向一致，与「写手能力不足」的方向不一致。

刺眼的一点：**post-MVP 的 yeban_guize 首过率 12%，是全库最低之一**，
低于它之前的 tangshuting_v1_backup（56%）。MVP 重构把候选数压到 1，
但返工循环（replan × structural_replan × beat_repair）原样保留，
于是搜索广度没了、补救深度还在。

**每章 22.7 次 LLM 调用中，只有 1 次是「第一次写正文」。其余 21.7 次都在补救。**

---

## 1. 为什么首过率低——三个根因，第一个已被实测推翻

### 根因 A（**已证伪，2026-07-27 实测**）：「写作调用推理被关掉」——不成立

原假设：三本在跑的小说都配置了 `writing_thinking_mode: disabled`
（`config_template` 另有 `writing_reasoning_effort: none`），
`CLAUDE.md` 记录的理由是 gemini-2.5-pro 在 nginx 前置的经销商网关后推理超过 ~70s 代理超时、
首字节前就 504。据此我判断「唯一出货的调用被阉割成零推理直出」，并把它列为首要杠杆。

**实测把它推翻了。** `tools/probe_reasoning.py` 对 `ai.littlesheep.cc/v1` + `deepseek-v4-pro`
（yeban_guize 的写作链路）跑完 8 档推理阶梯 × 2 组 max_tokens，共 11 次调用：

```
tier                     TTFB     total     out  reasoning   结果
effort=none (当前模板)    9.0s    190.5s    2301      7170   OK
effort=low               74.1s    168.2s       0      6115   预算被推理吃光
effort=medium             2.4s    155.2s    1728      6724   OK
effort=high              59.7s    225.0s       0      5956   预算被推理吃光
thinking=disabled ←生产    1.6s    138.3s    1919      5692   OK
thinking=enabled/4k      76.7s    241.6s       0      6053   预算被推理吃光
thinking=enabled/16k     23.3s    146.8s      98      6009   OK
(两个都不发)              12.7s    131.8s       0      6174   预算被推理吃光
```

三条结论，都与原假设相反：

1. **11/11 次调用都产生了 5,692–7,170 字符的推理**，包括生产实际发送的
   `thinking:{"type":"disabled"}`，也包括什么都不发。
   推理量与档位**无相关性**——网关根本不理这两个旋钮。
2. **一次 504 都没有。** TTFB 从 1.6s 到 76.7s 随机波动（含两次 >70s 仍成功），
   是网关排队噪声，不是推理深度。`CLAUDE.md` 里那条 ~70s 超时是 gemini-2.5-pro 时代的
   观察，对当前 deepseek-v4-pro 链路已不适用。
3. **所以不存在「把推理调回来」这个动作**——它从来没被调下去过。P1 的预期收益归零。

但探针挖出了一个更麻烦的事实。生产日志里 `reasoning_chars` 一直有记录，全库聚合后：

```
2026-07-14  375 次调用   69% 有推理
2026-07-21  1819 次      3%
2026-07-23  954 次      47%
其余 12 天   共 ~9800 次  0%
```

yeban_guize 的 107 次 write **全部 0 推理**；guize_guaitan 用**完全相同的配置键**，
plan_arbitrate 却有 65% 有推理。同一端点、同一模型名、同一配置，不同日子行为不同——
经销商网关把一个模型名路由到多个上游，推理与否由它决定。

**真正的结论：推理档位不是一个可控变量，而是一个未受控的混杂变量。**
它污染了这个仓库跑过的每一次 A/B——包括 `ab_short_chapter_score_inflation` 里那两次。
处置：`novel.py stats` 现在打印每个角色的实际推理覆盖率（`_reasoning_coverage`），
任何两次运行做对比之前先看这一行是否可比。要真正控制推理，只能换 endpoint / 换模型，
改配置无效。

### 根因 B（现在是首要原因）：每一次「选择」都在投票选平庸

当前每章约 10 次选择事件（6 臂策略老虎机 → 候选生成 → 候选预筛 → 6 轴融合评审 →
仲裁 → 候选正文评分择优 → review 接受/拒绝 → 结构诊断 → 重掷）。

外部研究对创作类任务的判定很明确：LLM 评审「偏好一个可信但无聊的版本，
胜过一个冒险但好的版本」（judges anchor on generic polish）。
[QuRating](https://arxiv.org/pdf/2402.09739) 与
[Judge Inflation](https://arxiv.org/pdf/2602.17170) 进一步证明 pointwise 打分
判别力低、分布压缩——实测吻合：1021 章自评均值 8.15，全部挤在 7.4–8.7，
失败书 5.64 与优质样本 8.69 同带，1021 章只有 6 次 `accepted=0`。

**所以：多候选 + LLM 择优 ≠ 取分布右尾，而是系统性地向均值回归。**
200 章 × 10 次选择 = 2000 次「投票选平庸」。

这解释了 tangshuting 200 章自评的单调塌缩，比任何其它假说都干净：

```
Ch0-20  8.49  →  Ch60-80  9.03  →  Ch100-120  7.65  →  Ch180-200  7.41
```

**搜索强度 22.7 调用/章，买到的是一条向下的曲线。**
在质量优先的目标下，这些搜索层不是「太贵」，是**有害**。网文要的是尾部（爆点），
不是均值；而每一个 LLM 选择器都在砍尾部。

### 根因 C（放大器）：上下文信噪比 + 规格精度

- 写作 prompt 中位数 **86,384 字符**；真正约束这一章的内容不到 5k。
  [context rot](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)：
  token 越多召回越差。你们自己的记忆条目 `doubao constraint salience` 已经发现
  「把高频违约的约束抬到 prompt 末尾锚点」有效——那正是在对抗信噪比，是症状不是治本。
- 规划 prompt 中位数 **174,635 字符**，是写作的 2 倍，只为产出 ~800 字符的计划。
  而且这 800 字符是三阶段委员会（候选 → 融合评审 → 仲裁）的**共识泥浆**——
  委员会的每一步都在磨掉具体性。规格越模糊，首过率越低。

---

## 2. 六个杠杆，按对首过率的预期影响排序

> **2026-07-27 更新：L1 已实测证伪并降级。** 下表的排序据此重排，
> L2/L3 上移为第一梯队。详见 §1 根因 A。

| # | 杠杆 | 预期影响 | 改动量 | 何时做 |
|---|---|---|---|---|
| ~~L1~~ | ~~恢复写作调用的推理~~ | **零（已证伪）** | — | **不做** |
| **L2** | **规格精度：可拍摄的章节卡片（删委员会）** | **高** | 中 | **P2（现为首位）** |
| **L3** | **上下文信噪比：86k → ~15k 结构化** | **高** | 中 | **P3** |
| L4 | 范例锚定：本书最佳章节原文作 few-shot | 中高 | 小 | P2 搭车 |
| L5 | 写前验证代替写后拒绝 | 中 | 小 | P4 |
| L6 | 修复代替重掷（L0/L1，永不整章重写） | 中 | 中 | P4 |

### ~~L1 — 恢复写作调用的推理能力~~（已证伪，保留记录）

原计划：换直连高超时 endpoint / 试 low-medium 中间档 / 换模型。
原判据：「如果 `none → medium` 能把首过率从 11% 抬到 40%，后面所有架构工作的收益都要重估。」

**实测结果：`none` 和 `medium` 发的是同一件事——网关两个都不理，11/11 次调用一律推理约 6k 字符。**
写手从来不是零推理直出，所以没有这个收益可拿。

留下的唯一可执行动作不是提高推理，而是**消除混杂**：推理覆盖率现在进了
`novel.py stats`，A/B 前必须确认两侧可比。真要控制推理只能换 endpoint / 换模型，
而那属于选型，不属于本设计的架构工作。

**这一项的真实价值是负的收益 + 正的信息**：它把「写手被阉割」这个混杂假设从后续每一步的
A/B 里摘了出去。原文说「不先做它，后面每一步的 A/B 结果都会被它污染」——
这句仍然成立，只是污染源不是配置，是网关。

### L2 — 规格精度：删委员会，改弧级节拍表

用**一次高推理调用**（每 ~10 章一次，可以奢侈）替代
`策略老虎机 → N 候选 → 预筛 → 6 轴融合评审 → 仲裁` 五段委员会。

产出 10 张 schema 约束的 `ChapterCard`，每张 ~300 字符，字段全部是**可拍摄的具体物**：

```json
{
  "ch": 27,
  "where": "县医院三楼旧档案室，凌晨两点，只有应急灯",
  "who": ["陈默", "值班护士林"],
  "wants": "拿到 1998 年那卷失踪病历",
  "blocked_by": "档案室的登记簿上，他自己的名字已经被人签过了",
  "turn": "签名是他的笔迹，但日期是三天后",
  "payoff": "回收 Ch19 埋的『笔迹』伏线",
  "exit_hook": "走廊尽头，护士林正在给某个人打电话，说的是他的名字",
  "forbid": ["再用『镜子』意象", "再用『他忽然明白了』式顿悟"],
  "opening_type": "physical_action"   // 与近 5 章不同
}
```

关键：`blocked_by` / `turn` / `exit_hook` 是**具体事件**而非抽象描述，
`forbid` 直接来自 ledger（已用套路台账）。写手拿到这张卡，几乎没有跑偏空间。

**为什么这提高首过率**：现在的计划是委员会共识，具体性被磨平，写手要自己补全，
补全就是发挥空间，发挥就是方差，方差就是重来。规格越具体，一次写对的概率越高。

### L3 — 上下文信噪比：单一 `StoryState`

12 份 markdown + 7 张表 + 4 个上下文构建器 → 1 个派生对象，~10–15k 字符：

```
facts    实体规范（谁是谁、当前状态）        ~2k
threads  伏线（开启章/到期章/状态）          ~1k
arc      当前节拍表 + 本章卡片                ~3k
recent   近 2 章摘要                          ~1.5k
voice    冻结的文风基线样本                   ~1.5k
ledger   已用套路/场景/开场类型                ~1k
rag      检索到的 top-3 历史原文片段          ~4k
```

由 append-only 事件日志纯函数投影得到，可缓存，字节稳定 → prompt cache 命中。

**连锁收益**：`memory_compress`（231 次/100 章）整个消失——派生状态不需要压缩；
`volume_plan_window` + `chapter_schedule_directive` 两个补丁消失——
节拍表是一等公民，不会被窗口化饿死。这正对中段塌缩。

#### 实测修正（2026-07-28）：**写作不是上下文预算的大头，规划才是**

`python tools/prompt_census.py`，全库 14,976 次调用 / 991M prompt 字符：

| tag | 调用数 | prompt 中位数 | 占总 prompt |
|---|---|---|---|
| `plan_candidate` | 2,500 | **131,872** | **33.7%** |
| `write` | 2,183 | 81,152 | 17.0% |
| `review` | 2,153 | 75,624 | 15.5% |
| `plan_review_fused` | 2,457 | 45,756 | 10.8% |
| `plan_arbitrate` | 1,268 | 55,866 | 9.2% |
| `extract` | 821 | 68,920 | 6.4% |

**规划类 tag 合计 53.8%，写作 17.0%。** 上面那句「写作上下文 86k → 15k」的
86k 是对的（实测中位 81k），但它只占总量的六分之一；而 `plan_candidate`
单个 tag 就占三分之一，且它的 prompt **比写手的还大**（132k vs 81k）。

这不推翻 L3 的设计（派生 `StoryState` 仍然是对的抽象），但**推翻了它的入口**：
先给写作路径换 `StoryState` 只能碰到 17%，还要付出 `cacheable_prefix` 全量失效的
代价（见 CLAUDE.md）。真要动，应当**从 `plan_candidate` 开始**——它同时是
prompt 体积第一名、返工主因（`replan` 占 33%–68% 的章）和 §4 里规划占 53% 成本的
同一个位置。这三件事指向同一个函数，不是巧合。

### L4 — 范例锚定：给写手看本书自己最好的章节

`exemplar_rag_enabled: false`，注释写的理由是
「un-IDF'd second ranker + per-write full-chapter disk I/O, redundant with rag」——
那是**工程理由，不是效果理由**。质量优先下应该修好并重开：

在写作 prompt 里放 **2 段本书评分最高章节的原文节选**（一段对白密集、一段动作/场景），
而不是抽象的「文风指南」。模仿具体样本的首过率显著高于遵守抽象规则。
这也是 [oh-story-claudecode](https://github.com/worldwonderer/oh-story-claudecode)
「对标拆解 → 文风.md / 节奏.md」的思路，以及冻结 `voice_baseline.md` 的正确延伸。

修法很简单：复用已有的 `retrieval.py`（有 IDF），从 chapter_metrics 里
按分数取 top 章节，只截取 800 字符片段，不读整章。

### L5 — 写前验证代替写后拒绝

卡片级检查是确定性的、零 LLM 成本的，而且**修一张坏卡片 ~2k token，
修一章坏正文要一次 write + 一次 review + 一次重掷**：

- 卡片与 canon 冲突？（人物已死 / 道具已毁 / 地点已封）
- 伏线超期未收？
- 场景骨架与近 5 章重复？（`scene_similarity` 降级到这里，只约束卡片，不触发重掷）
- 开场类型与近 5 章重复？

全部闸门前移到写之前。写之后只留能修的门。

### L6 — 修复代替重掷（永不整章重写）

每个门必须声明修复层，无修复动作的门直接删除（35 → 8）：

| 门 | 修复层 | 说明 |
|---|---|---|
| em_dash_density | **L0** 确定性改写 | `reduce_em_dash_density` 已存在 |
| fragment_ratio | **L0** 合并短句 | 文风塌缩首要指标 |
| fossil_phrase | **L0** 同义轮换 + 写前 avoid-list | `fossil_fix.py` 已有 |
| opening_dup | **L0** 换卡片里的备选开场 | — |
| length_band | **L1** 定点扩写/收束 | 只改问题段落 |
| dialogue_ratio | **L1** 定点对白注入 | 实测长期 0.116–0.164，偏低 |
| canon_conflict | **L2** 改卡片 | 一致性是硬约束 |
| thread_overdue | **L2** 改**下一张**卡片 | 不重写本章 |

**整章重写永久移除。** 重写丢掉的好东西比修好的多，而且重写的结果由
LLM 打分器择优 = 又一次向平庸投票。

> **P4 落地时的修正（2026-07-27）**：整章重写**保留**为 `score < 6.5`（分布最底部 6%）
> 的救援路径。理由见 §7「P4 落地记录」——把噪声章（7.x 段）从重写路径里摘出去
> 已经拿掉了绝大部分收益，而底部 6% 是真坏章，此时重写丢掉的东西确实比修好的少。
> 「永久移除」需要单独一次 flip 来证。

删除的 27 个门：判定标准是 (a) 无绑定修复动作，或 (b) 唯一动作是 retry，
或 (c) 实测 penalty 贡献 ≈ 0（现状 `style_penalty` 均值 0.00–0.20，
3782 行 quality.py 的总产出）。三条命中任一即删。

---

## 3. 明确的取舍：质量优先下，多候选反而更该删

这是最反直觉的一条，也是最重要的一条。

「质量优先 → 多生成几个挑最好的」是错的，因为**挑选器是坏的**：
- pointwise 打分实测无判别力（7.4–8.7 全带）；
- pairwise 虽然判别力更高，但对创作类任务，judge 系统性偏好「可信但无聊」；
- 因此 N 候选 + 任何 LLM 选择 = 向均值回归 = 砍掉尾部。

网文的价值在尾部（一个爆点章顶十个平稳章）。
**N=1 + 强先验（精确卡片 + 高信噪上下文 + 本书范例）
方差更大、期望更高、尾部更厚**，优于 N=3 + 弱先验 + 坏选择器。

省下的不是钱，是**被选择器砍掉的尾部**。

（原文这里写的是「N=1 + 强先验（**满推理** + …）」。§1 证伪后「满推理」不再是可选项——
写手本来就在推理，而且我们控制不了它。强先验只剩卡片、上下文、范例三项，
这反而让 §2 的 L2/L3/L4 变成了唯一的抓手。）

---

## 4. 预算重新分配：不是省，是搬

| 阶段 | 现状调用/章 | 现状 prompt/章 | 新设计调用/章 | 新设计 prompt/章 | 推理档位 |
|---|---|---|---|---|---|
| 弧级规划 | 6.92 | 827,000 | 0.10 | 8,000 | 每 10 章一次，可奢侈 |
| 卡片修复 | — | — | 0.15 | 2,000 | low |
| **写作** | 3.02 | 261,000 | **1.00** | **15,000** | 网关决定，不可控 |
| canon 一致性核对 | — | — | 0.30 | 4,000 | low |
| L1 定点补丁 | 0.04 | 3,000 | 0.30 | 2,000 | low |
| pairwise 对照 | 3.24（pointwise） | 253,000 | 0.10 | 8,000 | medium |
| 抽取/状态 | 2.00 | 161,000 | 0.00（并入 write 的 delta 输出） | 0 | — |
| 记忆压缩 | 2.31 | 2,000 | 0.00 | 0 | — |
| 其它 | 5.20 | 57,000 | 0.05 | 1,000 | — |
| **合计** | **22.7** | **1,564,000** | **~2.0** | **~40,000** | |

> **2026-07-27 更新**：原文说「关键不是那两个总数，是最后一列（推理档位）」。
> §1 证伪后，最后一列**不再是一个可以设计的变量**——档位是请求，网关不保证兑现。
> 于是关键回到了倒数第二列：**写作上下文从 86k 噪声降到 15k 高信号**，
> 这是仍然成立、且有文献支撑（context rot）的收益。

省下的算力去向：弧规划一次做深、一致性核对新增、pairwise 新增。
「写作满推理」这一项从收益表里划掉。

---

## 5. 目标架构

```
BRIEF (prompt.md, 不可变)
   │  bootstrap ×1
   ▼
CANON  = append-only event log（SQLite，唯一真相源，每章追加 1 条 ChapterDelta）
   │  纯函数投影（零 LLM，可缓存，字节稳定 → prompt cache 命中）
   ▼
StoryState ≈ 15k  ← 唯一上下文对象
   │
   ├─► ARC PLANNER   每 ~10 章 1 次，max reasoning → 10 张 ChapterCard
   │        │
   │        ▼
   │   写前确定性验证（canon 冲突 / 伏线超期 / 场景重复 / 开场重复）
   │        │ 冲突 → 卡片修复（1 次 low 调用）
   │        ▼
   ├─► WRITER        1 次，**max reasoning**，15k prompt
   │   输入：StoryState + ChapterCard + RAG top-3 + 本书 top 章节范例 ×2
   │        + 约束锚点（置于 prompt 末尾）
   │   输出：prose + ChapterDelta（一次调用同时给出，不再单独 extract）
   │        │
   │        ▼
   │   L0 确定性修复（破折号 / 片段率 / 化石 / 开场）        [0 调用]
   │        ▼
   │   8 个门 → 需要 L1？（预期 ~30%）→ 定点补丁 [1 次 low 调用, 2k]
   │        ▼
   └─► CANON.append(ChapterDelta) → StoryState 失效重算        [0 调用]

每 K 章：pairwise 对照（新章 vs 本书冻结参考章）→ 更新 writer directives + 范例池
```

### 模块职责（7 模块，~4,000 行；现状 25,654 行 / 23 模块）

| 模块 | 行数 | 职责 | 明确不做 |
|---|---|---|---|
| `canon.py` | 400 | 事件日志；`StoryState` 纯函数投影 + 缓存；md 投影仅供人读 | 不做 LLM 压缩、不做 4 层预算 |
| `arc.py` | 300 | 弧级节拍表（max reasoning）；卡片写前验证与修复 | 不做候选/筛选/仲裁/老虎机 |
| `write.py` | 600 | prompt 组装（含范例锚定 + 末尾约束锚点）；单次调用出 prose + delta | 不做多候选采样 |
| `gates.py` | 500 | 8 个门，每门必须声明 `repair: L0/L1/L2` | 不做无动作的 advisory 门 |
| `fix.py` | 400 | L0 确定性文本变换；L1 定点补丁 | **不做整章重写** |
| `judge.py` | 250 | 每 K 章 pairwise；维护参考章与范例池 | 不做逐章 pointwise 打分 |
| `run.py` | 500 | 主循环、checkpoint/resume、并发、CLI | — |
| `llm.py` / `retrieval.py` | 保留 1,959 | 流式/超时/salvage/JSON 修复；无依赖 TF-IDF RAG | — |

配置键 452 → ~40（API 路由 15 + preset 1 + 8 个门阈值 16 + 节奏 8）。
其余下沉为代码常量：**Code over Config**，不需要用户调的东西不该是旋钮。

---

## 6. 与现状对比

| | 现状 | 新设计 |
|---|---|---|
| **端到端首过率** | **12%–63%（中位 38%）** | **目标 ≥70%（待实测）** |
| 首要返工原因 | `replan`（占章数 33%–68%） | 无 replan 路径（卡片写前修，正文写后补） |
| 写作调用推理档位 | **不可控（网关决定，0%–69% 随日期波动）** | 同左——不作为设计变量，只作为可见的混杂项 |
| 写作上下文 | 86k（信噪比低） | 15k（结构化高信号） |
| 每章调用 | 22.7 | ~2.0 |
| 每章 prompt | 1,564k 字符 | ~40k 字符 |
| 每章 LLM 选择事件 | ~10（每次投票选平庸） | **1**（仅 pairwise，且不做逐章择优） |
| 整章重写 | 有 | **无** |
| 代码 / 模块 / 配置键 | 25,654 / 23 / 452 | ~4,000 / 7 / ~40 |
| 确定性门 | 35（0 个绑修复动作） | 8（全部绑修复动作） |
| 百章墙钟 | 71.3 小时 | 预估 8–12 小时 |

---

## 7. 执行顺序（每步独立可发布、独立可实测）

A/B 纪律（沿用 `ab_short_chapter_score_inflation` 的教训）：
**中段问题区 Ch20–50 · 同 prompt · 等长目标 · 单变量 · ≥30 章**，
用现成的 `novel.py ablate` + `novel.py compare`。
**新增一条：比较前先看 `novel.py stats` 的推理覆盖率行，两侧不可比就不要比**（§1 根因 A）。

| 阶段 | 内容 | 主指标 | 判据 | 状态 |
|---|---|---|---|---|
| **P0** | FPY 进 `novel.py stats`（从 checkpoint 产物精确统计，非 `llm_calls.jsonl` 估算） | — | 首过率可见 | ✅ **已完成** |
| **P1** | L1：实测写作链路的推理档位阶梯 | FPY | 找到 504 之下的最高档 | ❌ **已证伪**：网关无视档位，11/11 次一律推理 ~6k 字符；无档位可换。副产物：推理覆盖率进 stats |
| **P2** | L2：`arc.py` 弧级卡片替代五段委员会（L4 范例锚定拆出，见下） | FPY + 成本 | FPY 不降且成本 −53% 即通过 | ⚠️ **未通过判据；代码保留，默认关闭**（见下） |
| P2b | L4：范例锚定（`exemplar_block` 已重写：改秩排名选择 + 对白/动作双范例 + 保留段落缩进） | 章评分 + 文风罚分 | 需自己的单变量 A/B | ⏸ 两臂均关闭，等 P2 结论后单独测 |
| **P3** | L3：`canon.py` + `StoryState`，删 `memory_compress` 与 4 个构建器 | 中段 Ch30–50 的 FPY 与一致性错误数 | 中段曲线不再下滑 | 🔄 **已开工，入口按实测改到 `plan_candidate`**：指纹库改聚合，Ch201 plan prompt **116,592 → 95,157（−18.4%）**，且该块由 O(书长) 变 O(1)。`StoryState` 本体未动工（见下） |
| **P4** | L5 + L6：确定性返工触发器 + `fix.py` L0/L1 修复阶梯（整章重写按用户决定**保留**为 <6.5 的救援路径；门的删除已按 `gate_census` 数据单独做，见 §7 门校准） | FPY + 重来次数 | 调用数/章下降 **且** 匹配章 pairwise 胜率 ≥50% | ❌ **未通过：成本侧反向**（16.25 vs 14.75 调用/章）。代码保留，`rework_trigger` 保持默认 `score`。失败机理已定位并修复（见下） |
| **P5** | 删逐章 pointwise，接 pairwise | pairwise 胜率 vs 参考章 | 胜率 >55% | 📐 **未动工**。前置条件是 P4 的结论：pointwise 自评正是 P4 在拆的那条无判别力信号，先确认去掉它的返工权之后书还站得住，再谈换成 pairwise |

### P2 A/B 结论（2026-07-28）：成本收益成立，质量收益未证实 ⇒ 保留代码，默认关闭

`yeban_guize` @Ch25 分叉，`ab_committee`（委员会）vs `ab_arc`（弧级卡片），
唯一配置差异 `arc_planning_enabled`。原计划各写 Ch26–55，实际按用户要求
**在 Ch28 停机**，所以判据是在 **n=3** 上读的——这本身就低于本文件开头那条
「≥30 章」的纪律，结论必须按「未证实」而不是「已证伪」记。

报告：`experiments/ab_committee_vs_ab_arc_ch26-28.md`

| | ab_committee | ab_arc |
|---|---|---|
| 均分 | 6.35 | 6.36 |
| 最低分 | 5.80 | **4.80** |
| 有 replan 的章 | 2/3 | 3/3 |
| force-accept | 2 | 3 |
| gate_reject | 4 | 5 |
| LLM 分钟/章 | 74.33 | **57.15** |
| 规划占 LLM 时间 | 0.69 | **0.41** |

- **成本侧收益是真的**：规划时间占比 0.69 → 0.41，每章 −23%。这正是 L2 想买的东西。
- **质量侧没有收益**：均分差 0.01（噪声），而弧臂的**尾部更差**（min 4.80 vs 5.80）、
  replan 更多、force-accept 更多。两臂 FPY 都是 **0%**——问题区里两种规划都救不回来。
- 判据是「FPY 不降 **且** 成本 −53%」。FPY 两侧同为 0 无法区分，成本 −23% 也没到 −53%。
  **不通过。**

处置：**保留 `arc.py`，`arc_planning_enabled` 保持默认 `false`。** 理由是它的集成面
只有 `planning.create_plan` 里的一个接缝、`plan_from_arc` 永不抛异常、失败即回落委员会
（见 CLAUDE.md「Arc planner」），留着不带运行时成本；而 n=3 也不足以支持删除。
真要下结论，需要一次 Ch20–50、≥30 章、且两侧推理覆盖率可比的重跑。

### P3 第一刀（2026-07-28）：先把 `plan_candidate` 拆开量，再动 `StoryState`

按 L3 的实测修正，入口从写作改到了 `plan_candidate`。动手前先把一条**真实**的
Ch201 plan prompt 抓下来按 `^##` 分节归因（patch `planning.call_llm`，让第一个候选抛
异常，零 LLM 成本），结果见 `docs/LESSONS.md` §8。两条结论决定了先改什么：

1. **`mem` 是被 cap 住的，动它一点都省不到。** 每本配置都写 `plan_memory_chars: 60000`，
   在这个预算下只装得进 tier1 + 被截断的 tier2，`memory_context` 提前返回，
   tier3/tier4 根本到不了这个 prompt。所以上一步做的 tier 去重（LESSONS §12）是
   真的去重，但它落在 `extract` 和其它不设上限的消费者上，**没有落在最大的那个
   prompt 上**——在 `mem` 内部瘦身只会让截断点往后挪，总量还是 60k。
   这条要记住：`plan_candidate` 的可优化面积只有 `mem` 之后 planning.py 自己拼的那 35k。
2. 那 35k 里最大的一块是 `fingerprint_block`，**22,813 字符 / 全 prompt 的 19.6%**，
   而且每写一章长一行。它交付不了自己表头承诺的信号：全书 200 章有 194 条互不相同
   的流程，整条流程级别的重复根本不存在。改为输出 move 词表层面的聚合
   （高频 bigram/trigram + 类型频次）后 1,240 字符，对书长 O(1)。

这一刀的性质与 §7「门校准」同类：**不是删功能，是把一个从未测过分布的口径对齐到
实测分布上**。

**这里要更正本节的第一版（以及 commit `adfb545` 的 message）**：那版写「判别功能仍在
`check_plan_against_fingerprints` 手里，它一直是真正干活的那个」。这句是错的。
grep 显示该函数只被 `quality.py` 自己和它的单测引用，**生产路径从未调用**；再按 6 本
437 章回放它的阈值，复合相似度最高只到 **0.448**，而 `fingerprint_warn_threshold`
是 **0.65** ——与已删的 `dialogue_pingpong` / `chapter_ending_quality` 同一种缺陷
（阈值高于实测分布，结构上不可达；唯一让它超过 0.65 的单测是拿计划跟自己比）。
于是它连同 62 行一起删掉了，`fingerprint_warn_threshold` 也随之没有读者。
真正在管计划骨架重复的是 `scene_similarity`，它会触发。
`store_chapter_fingerprint` 保留：`skeleton_tokens` 这一列正是上面那次离线回放的
数据来源，未来重校准还要靠它。

`canon.py` / `StoryState` 本体仍未动工，且不应急于动：它要付 `cacheable_prefix`
全量失效的代价，而上面这类零代价的口径修正还没穷尽。

### P4 落地记录与对原设计的偏差（2026-07-27）

动手前先测了一件原设计没测的事：**返工率是门槛位置的函数，不是质量的函数。**
`quality_threshold: 8.0` 是 revise / replan / force-accept 的统一触发线，
而全库 1023 章自评的 **median 恰好 8.00**（mean 7.91，below 8.0 占 34%，below 6.5 只占 6%）。
门槛压在中位数上，三分之一的章就必然进返工循环——而这条自评已被证明无判别力
（§1 根因 B），所以 7.6 与 8.1 之间是噪声。**当前系统在对噪声做结构性重做。**

644 份归档 review 的门级普查（现已固化为 `tools/gate_census.py`）给出第二个结论：
penalty 总量只有 **0.41/章**，集中在 6 个门，5 个门在 600+ 次运行里一次都没触发。
所以**出血点是触发器，不是门的数量**——把章压到 8.0 以下的是 raw score，不是 penalty。

据此对原设计做了三处偏差，都是有意的：

| 原设计 | 实际落地 | 理由 |
|---|---|---|
| 门 35 → 8 | **一个门都没删**，只给每个门声明了修复层（`repair=L0/L1/L2/advisory`） | 普查显示删门买不到运行时收益；5 个零触发门里 `adjacent_repetition` 有仓库记录的历史命中（suspense_v11 Ch3 overlap 0.73），当前样本只是没覆盖到。删除决策交给 `gate_census.py` 的数据在后续阶段做 |
| **整章重写永久移除** | 保留为 `score < rework_score_floor`(6.5) 的**救援路径** | 那是分布最底部的 6%，是真坏章而不是噪声章；「永久移除」作为独立的第二次 flip 单独测 |
| 化石轮换 = L0 确定性修复 | L0 **只轮换替换表内的短语** | 实测 647 份 review 里 109 个化石短语，替换表覆盖的是通用陈词（`声音压得很低` 49 次），其余是书内专名（`老市场街七号` 42 次）——轮换专名是 canon 污染，不是修复 |

离线回放（`tools/replay_l0.py`，零 LLM）：29 章命中 L0，**0 章变差，长度变化全部在 ±2% 内**。
L1 真正要承担的工作量按 planned 计数是 `expand_to_band` 226 > `em_dash_targeted` 41 >
`inject_dialogue` 30；`fossil_rotate` 虽然 planned 493 次，实际可执行只有 26 次（上表第三行的天花板）。

A/B 判据必须防自放水：FPY 会因为「返工」定义变宽而机械上升，单看 FPY 无效。
通过条件 = 调用数/章下降 **且** 在「score 臂做了 replan、det 臂跳过了」的匹配章上
pairwise 胜率不低于 50%。任一不满足则回退。

原文写「P1 必须先做且单独做，它可能是全部收益里最大的一块」。
**先做是对的，判断是错的**——它的收益是零，但它花两小时排除了一个会污染后面每一次
A/B 的假设。P2 现在是收益最大的一步，而且 §0 的实测（返工主因是 `replan`，不是写作）
正好指着它。

### P4 A/B 结论（2026-07-28，n=4）：**未通过——成本侧反向**；机理已定位并修复

`tangshuting_e2e` @Ch46 分叉，`p4_score` vs `p4_det`，唯一差异 `novel.rework_trigger`。
两臂**都在 Ch50 停**：`p4_det` 先撞到 `target_words`（226,992/225,528 = 100.65%）自然
`Book complete`，`p4_score` 随后手动停在同一章边界。所以可比窗口是 **Ch47–50，各 4 章**
（前 46 章是继承的同一批文本，全书口径会被它们稀释成噪声——
`experiments/p4_score_vs_p4_det.md` 那份全书报告的 46/50 行两臂逐字相同，
它的 heuristic verdict 是在继承段上算的，不要读它）。
窗口报告：`experiments/p4_score_vs_p4_det_ch47-50.md`。

| Ch47–50 | p4_score | p4_det |
|---|---|---|
| 自评 47/48/49/50 | 8.2 / 8.0 / 8.2 / 6.2 | 6.9 / 7.4 / 6.8 / 7.4 |
| LLM 调用（4 章） | **57** | 65 |
| 调用/章 | **14.25** | 16.25 |
| LLM 分钟/章 | **22.91** | 28.73 |
| 规划占 LLM 时间 | **0.33** | 0.49 |
| force-accept | **1** | 2 |

> **上表的调用数修正过一次（2026-07-28）。** 初版写的是 59 / 14.75，多算了 10 行：
> `tools/pairwise_ab.py` 借 A 臂的 config 拿 API key，`call_llm` 于是把评委自己的
> `pairwise_ab` 调用写进了 `novels/p4_score/logs/llm_calls.jsonl`——而那正是
> `compare._llm_totals` 读的文件。**被测臂因为「被测量」而被记了成本。** 两处都修了：
> 评委改写到 `experiments/pairwise_logs/`（`dataclasses.replace(paths, logs_dir=…)`），
> `compare.py` 增 `OFFLINE_TOOL_TAGS` 过滤并在报告里显式打印被排除的行数（旧日志里
> 的行还在，所以读侧的过滤是必需的，不是冗余）。修正后成本差距**变大**，结论方向不变。

**判据第一半（调用数/章下降）不成立，方向还是反的 ⇒ 不通过 ⇒ `rework_trigger`
保持默认 `score`。** 判据第二半按预案跑了（`tools/pairwise_ab.py`，双序去位置偏、
不带 `cacheable_prefix`、盲标甲/乙）：

```
Ch48: order1=a order2=b -> tie
Ch49: order1=a order2=b -> tie
A=0  B=0  tie=2   B 非劣 50%   position-flips 2/2   raw votes A=2 B=2
```

只有 2 章的返工真正分叉（Ch47 两臂同为 revise×1，Ch50 两臂同为 replan+debt），
而这 2 章**两序全翻**——评委每次都选排在前面的那一个。技术上「非劣 ≥50%」达成，
但 flip 2/2 说明这次测量**没有信息量**，不能拿它当第二半的通过证据。
即：det 臂放行的 6.8–6.9 章，读起来既不比 score 臂差、也没被证明不差。

#### 为什么「少返工」反而更贵：两条规则在抢同一个无判别力的分数

日志给出的机理是干净的、可复现的，不是噪声：

```
p4_det  : Risk upshift Ch48/Ch49/Ch50 — min_recent_score=6.9/6.9/6.8 < 7.0   → 3 candidates ×3 章
p4_score: Risk upshift 只在 Ch51（窗口外）触发                                → 窗口内 0 次
```

`_effective_candidate_count` 的 RISK UPSHIFT 用 `risk_upshift_score_floor: 7.0`
读 `chapter_metrics.score`；而 `rework_trigger: deterministic` 的全部意义是
**不再把 7.x 当缺陷**、直接放行。于是 det 臂放行 6.8 → 下一章被判「塌缩中」→
扩宽候选。**它省掉的 1 轮 revise，远便宜于它买来的规划宽度。**这不是「确定性返工
不好」，是两条规则在同一根无判别力的自评轴上朝相反方向拉。

按标签数的实测（`llm_calls.jsonl`，Ch47–50，已排除 `pairwise_ab`）说明贵在哪里——
**不在写作，在规划**：

| tag | p4_score | p4_det |
|---|---|---|
| `plan_candidate` | 8 | **13** |
| `plan_review_fused` | 3 | **12** |
| `write` | 8 | 7 |
| `revise` | **4** | 2 |
| `review` | **14** | 9 |

det 臂确实少写少评了（revise 4→2，review 14→9，正是触发器想要的），但
`plan_candidate + plan_review_fused` 从 11 涨到 25。原因是升宽只加**计划**候选
（`planning.py:1747`，`_effective_candidate_count` 唯一的调用点，不动
`candidate_chapters`），而单候选会**整段跳过**融合计划评审
（`Skipping fused plan-review Ch48: single candidate`）——所以 1→3 个候选不是
「多 2 次」，是「从 0 次评审变成 3 次评审」。规划占比 0.33→0.49 就是这一条。

修复（`planning._risk_score_floor`，`tests/test_fix.py:RiskUpshiftFloorTest`）：
`deterministic` 模式下风险地板取 `min(risk_upshift_score_floor, rework_score_floor)`
= 6.5，理由与「`rework_score_floor` 对齐 `circuit_breaker_score_floor`」完全同一条：
**被触发器判为正常的章，不能同时被第二条规则判为险情。** `score` 模式逐位不变。

未结算的部分要说清楚：这次修复**没有重测**。它把 P4 的下一次重跑从
「测触发器 + 测触发器与扩宽的耦合」变成「只测触发器」，但它本身仍是一个未经 A/B 的
一致性修正（默认模式下完全惰性，所以入库风险为零）。

#### 判据换成可判定量：FPY′（`tools/fpy_prime.py`，零 LLM）

上一段说「重跑之前先把判据从自评换成可判定量」，这一步已经做完了。

`novel.py stats` 的 FPY 数任何返工痕迹（revise / replan / debt / hook 重写），
而这些痕迹**全部**由 `score < quality_threshold` 产出——所以 P4 一改释放规则，
FPY 在两臂都机械地动，它结构上无法结算 P4。FPY′ 换一个自评碰不到的问题：

> **第一稿、第一份计划，有没有携带一条「被测量到」的缺陷？**

- 「被测量到」= `pipeline._hard_block_reasons`，引擎自己那张「这稿作废」清单
  （gate_rejects / 文体塌缩 / 硬矛盾 / 硬契约 / length_band·opening_hook 拦截 /
  邻章复读拦截 / 未满足约束堆积）。**其中没有一条读 `score`。**
- 判据阈值**钉死在引擎默认值**（`fpy_prime.PINNED`），不读各臂自己的 config——
  否则两臂配置一分叉，尺子就跟着分叉。
- 只读 `review_round0.json`（第一稿的评审）。`final_review.json` 描述的是修完之后
  的文本，回答不了「首过」。
- 计划重试只认**写前且确定性**的三个 label：`plan_initial_attempt[1-9]`、
  `plan_critical`（连续性 CRITICAL）、`plan_fossil_catastrophe`（全候选被化石门拦）。
  `plan_quality_replan` / `plan_hard_floor` **明确排除**——它们是返工决策的下游，
  算进来就把释放规则又塞回判据里，正是 FPY′ 要拆掉的那个循环。
- 缺 round-0 评审的章报 `n/a`，不当成通过。

P4 窗口重新结算（`python tools/fpy_prime.py p4_score p4_det --from 47 --to 50`）：

| Ch47–50 | FPY′ | 自评 ≥8.0 | 首稿失败原因 |
|---|---|---|---|
| p4_score | 2/4 **50%** | 0/4 | Ch48 `style_collapse(penalty=3.0)`、Ch50 `gate_rejects=book_wide_fossils_ratio` |
| p4_det | 3/4 **75%** | 0/4 | Ch50 `gate_rejects=book_wide_fossils_ratio`（同一条） |

**但这 25 个点是买来的，不是白得的。** 唯一分叉的是 Ch48：score 臂 1 个计划候选
（跳过计划评审）出的首稿文体罚分 3.0，det 臂在 Ch48 恰好被升宽到 3 个候选、过了
3 次融合评审，首稿罚分 0.8。FPY′ 的全部差距落在那一章，而那一章正是成本反向的来源。
所以结论没有变：**判据第一半不成立，第二半的收益与第一半的超支是同一笔钱。**
`rework_trigger` 保持默认 `score`。

顺带得到一个可检验的预测：`_risk_score_floor` 的修复会取消 Ch48 的升宽，因此重跑
应当看到 det 臂**成本降到 score 臂以下、FPY′ 回落到与 score 臂持平**。若重跑后 det
臂 FPY′ 仍高于 score 臂而成本更低，那才是 P4 真通过；若 FPY′ 跟着掉回去，说明确定性
触发器的收益本来就只是「省了 revise」，两边打平，代码继续默认关闭。

FPY′ 的全库读数（顺手拿到的额外结论）：

```
huangliang     94/100  94% | tunshi_xitong 44/52 85% | tangshuting_v1_backup 157/200 78%
yeban_guize    20/26   77% | tangshuting  151/199 76% | guize_guaitan  9/13  69%
tangshuting_e2e 28/45  62%
首稿失败原因合计：hard_contract 54 · gate_rejects 39 · replanned:initial 28 ·
              style_collapse 25 · hard_contradictions 11 · replanned:critical 2
```

对比同一批章的「自评 ≥8.0」比例（12%–63%），FPY′ 的区分度更高（62%–94%）**且
每一次失败都指得出具体门**。两个头号杀手的分布很不一样，别混着读：

- `hard_contract` 54 次，**摊在 8 本里**（20/16/7/5/2/2/1/1）——这是全库性的，
  首稿最常栽在硬契约上，而不是栽在文体或重复上。
- `gate_rejects` 39 次里有 **27 次是 tangshuting 一本**，`replanned:initial` 28 次
  里有 15 次是 `tangshuting_e2e` 一本。这两条是单本病灶，不是全库结论。

所以下一刀的落点是硬契约的首稿合规率，而不是继续调门槛——与
`docs/REDESIGN_V2.md` §0 的判断一致。

---

## 8. 诚实的不确定性

- **~~写作调用当前 reasoning 全关，这是可测量的能力损失。~~** 已证伪（§1 根因 A）。
  写手一直在推理，我们既没关掉它，也开不大它。剩下**唯一**有文献支撑的确定项是：
  上下文 86k 触发 context rot，这是可测量的召回损失。
  **因此 FPY 目标的信心比原文低**——原来是「两个已知损失都修好」，现在是「一个」。
- **删多候选是本设计里最激进的一步。** 论据是「LLM 选择器对创作任务偏好平庸」
  （有研究支撑）+ 「22.7 调用/章没阻止 9.03→7.41」（有实测支撑），但它是推断，不是实验。
  P2 会直接检验它：如果删掉委员会后 FPY 和 pairwise 胜率都下降，这条推断就被证伪，
  应当回退到「N=2 + pairwise 择优」而不是回到现状的五段委员会。
- **P1 的证伪反过来加强了根因 B/C。** 既然写手的能力没有被配置削弱，
  那么 88% 的算力花在补救上、以及 200 章 9.03→7.41 的滑坡，就不能再归因于「写手弱」。
  只剩两个解释：规格不精确（B/C）和选择器投票选平庸（B）。
- **网关的不确定性无法用架构解决。** 同一配置在不同日子给出 0% / 47% / 69% 的推理覆盖率，
  这是供应商问题不是设计问题。所有 A/B 都必须带上这一列数据一起读。
- **`exemplar_rag` 当初被关有其工程理由**（无 IDF 的二级排序 + 整章磁盘 IO）。
  重开必须连同修好，而不是简单翻开关。

---

## 9. 一句话

当前系统把 **~90% 的算力用来补救**，补救过程中做了约 10 次「投票选平庸」的选择，
结果是 200 章自评从 9.03 单调滑到 7.41、首过率中位 38%（post-MVP 那本只有 12%）。
返工的主战场不是写作，是规划——每一本的头号返工原因都是 `replan`。

新设计只做一件事：**把算力从补救搬回创作。**
写作那一次调用拿到 15k 高信号上下文、一张可拍摄的卡片、两段本书最好的原文；
其余全部删掉或降级为零 LLM 的确定性修复。
（原文这里还写着「开满推理」——实测证明那不是我们能给的东西，已删。）
